{
  "name": "client",
  "private": true,
  "scripts": {
    "build:android": "bun cap copy android && cd android && ./gradlew assembleDebug",
    "test:android": "bun cap run android",
    "test": "bun run serve"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.0.2",
    "serve": "^14.2.5"
  },
  "peerDependencies": {},
  "dependencies": {
    "@capacitor/android": "^8.0.2",
    "@capacitor/core": "^8.0.2",
    "@capacitor/device": "^8.0.0"
  }
}
